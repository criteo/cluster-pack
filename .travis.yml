language: generic
os: linux
python:
  - 3.6

before_install:
  - env
  - pip --version
  - python3.6 -h
  - python3.6 -m venv test_env
  - test_env/bin/activate
  - pip install conda-pack

install:

script:

notifications:
  email: false