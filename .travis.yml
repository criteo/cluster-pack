language: python
dist: trusty
os: linux
python:
  - "3.6"

before_install:
  - env

install:
  - pip --version
  - pip install .
  - pip install pytest
  - pip freeze

script:
  - pytest -s tests

notifications:
  email: false