[pytest]
# pylama ships a pytest11 plugin declaring the `path` arg of pytest_collect_file,
# removed in pytest 9.1. pylama is unmaintained and we lint via the `pylama` CLI,
# so block its plugin instead of pinning pytest.
addopts = -p no:pylama
markers =
    hadoop: tests with hadoop-test-cluster 
